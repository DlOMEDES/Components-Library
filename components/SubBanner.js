import styles from "../styles/subBanner.module.scss";

const SubBanner = () => {
  return (
    <div className={styles.subtitle}>Reusable components for Web Apps</div>
  );
};

export default SubBanner;
